# Path: tests/**

- Test names describe behavior, not implementation (e.g. `rejects_expired_token`, not `test1`).
- Every bug fix must ship with a regression test.
- No skipped/disabled tests merged without a linked ticket explaining why.
- E2E tests must not depend on production data.
