# Path: prototypes/**

- Relaxed standards — prototypes are throwaway by default.
- Each prototype needs a short README stating its hypothesis and what "success" would look like.
- Prototype code must not be imported into src/ directly — promote by rewriting to meet standard rules.
