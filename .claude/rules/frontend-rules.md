# Path: src/frontend/**

- Components must not fetch data directly outside of designated data-layer hooks/services.
- No inline magic numbers for spacing/color — use design-system tokens.
- All interactive elements must be keyboard-accessible and carry appropriate ARIA attributes.
- New components require a unit/component test in the same PR.
- Bundle-affecting dependencies require frontend-lead approval.
