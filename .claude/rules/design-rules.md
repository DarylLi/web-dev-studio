# Path: design/**

- Every screen spec must cover: default, loading, empty, error, and success states.
- Must reference design-system tokens/components rather than one-off values.
- All UI design deliverables (design system, wireframes, mockups) must be viewable HTML files
  (`design/design-system.html`, `design/wireframes/*.html`, `design/mockups/*.html`) —
  not text-only descriptions. Open them in a browser to verify before sign-off.
- Contrast ratio must meet WCAG 2.1 AA before handoff to engineering.
