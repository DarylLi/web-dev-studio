# Path: docs/api-spec/** and src/backend/api/**

- Every endpoint must be documented before merge (request, response, error cases).
- Versioning: breaking changes get a new version; additive changes don't.
- Consistent error shape across all endpoints.
